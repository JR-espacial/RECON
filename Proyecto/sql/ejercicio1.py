def maxSum(A):
    max = A[0]
    for(i in range(len(A))):
        for(j=i in range(len(A))):
            sum = sum + A[j]
            if(sum > max):
                max = sum
return sum

# Ejercicio 2

def sequence(T, N, result[])
    if(len(result) >= N)
     return result = []
     
    for(i in range (len(T)):
        for(j in range (len(T))):
            if(empty(T[T[i][j]])):
                result.append(T[i][j])
                T.pop(T[i][j])
return result








